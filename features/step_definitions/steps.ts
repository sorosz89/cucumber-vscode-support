import { Given, When, Then } from '@cucumber/cucumber';

Given('today is Sunday', () => 'pending');

When('I ask whether it\'s Friday yet', () => 'pending');

Then('I should be told {string}', (string) => 'pending');